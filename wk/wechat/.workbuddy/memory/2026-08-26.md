# 2026-08-26 工作日志

## 发布 v2.7.0（A/C/D/B 四大特性 → 生产后端 + 小程序）

### 完成（已提交）
- 后端 `card-counter-flask`：A/C/D/B 特性已并入生产后端（此前只在 demo 仓 commercial）。
  - commit `2e8c853b`（5 个文件：__init__.py / model.py / commercial.py / commercial_views.py / test_commercial_integration.py）
  - 测试 `test_commercial_integration.py` **31/31 通过**
  - 新增路由：/api/orders, /api/orders/<id>/pay, /api/orders/mine, /api/merchant/segments, /api/merchant/cards/<id>/refund-estimate, /api/cards/<id>/refund-estimate, /api/group-buy/redeem, /api/templates/<id>
  - 启动期幂等迁移：CardTemplate 退费列 + Order.ref_template_id + 新表 GroupBuyVoucher
- 小程序 `card-counter-miniapp`：顾客端 UI 完成（commit `f3f05a3e`）
  - 新增 pages/order/buy（买卡落地页）、pages/groupbuy/redeem（团购核销）
  - card/detail 接入应退估算；card/list 增加「团购券核销」入口；profile 增加「购买次卡」入口
  - app.json 注册两新页面；版本号 2.5.2 → **2.7.0**（v2.6.0 已在历史记录用过）
  - 修复 detail.wxml 中 refundCents 误用顶层字段 → 改为 refund.refundCents
  - JS 文件 node --check 全部通过
- 新增 `card-counter-flask/cloudbaserc.json`（commit `ec08f48b`）用于 cloudrun 部署

### 发布动作被平台限制阻塞（未成功）
1. **小程序上传失败**：`node scripts/upload_ci.js 2.7.0 ...`
   - 报错 `errCode -10008 invalid ip: 2408:820c:...`（当前机器 IPv6 不在 private.key 白名单）
   - 解决：在微信 MP 控制台「开发管理→开发设置→小程序代码上传IP白名单」加入当前 IP，或从生成 private.key 的原网络上传
2. **后端 Cloud Run 部署失败**：`tcb cloudrun deploy`
   - 已升级 @cloudbase/cli 3.6.1 → 3.8.1（3.6.1 的 `tcb run deploy` 有 env 参数解析 bug，新命令 `tcb cloudrun deploy` 正常）
   - `tcb cloudrun list -e cardcount-d4gjfjexz3097d803` 返回 **0 个服务** → 「云托管资源未开通」
   - 服务名 `flask-z9hh` 在 CLI 视角不存在（detail 报 service data illegal）
   - 结论：当前 tcb 登录账号/地域下该 env 没有云托管服务，或后端实际部署在别的账号/env/机制
   - 需用户确认：正确的 envId / 是否需先在控制台开通容器型云托管 / 真实服务名

### 待用户决策
- 小程序：加 IP 白名单后重传，或换白名单内机器上传
- 后端：确认云托管 envId 与服务名；或改用控制台/其他账号部署
- 两项完成后再补打 tag v2.7.0

## 环境工具备注
- miniprogram-ci 已 `npm install --no-save` 到 card-counter-miniapp/node_modules（1082 包）
- @cloudbase/cli 全局已升 3.8.1（/home/wang/.npm-global/bin/tcb）
- tcb 云托管命令：`tcb cloudrun deploy -e <envId> -s <svc> --source <dir> --port 80 --force --wait`
