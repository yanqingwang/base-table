# 2026-08-21 工作日志

## 次卡管家 P2 行业模板一键开店（card-counter-commercial）

- 继续「请继续」推进 P2（方向4 体验补强），P0/P1 已于 08-20 全完成（44/44）。
- **模型**：`Merchant.industry`、`CardTemplate.industry/presetKey/allowFamily`、`Card.shared`、新表 `CardMember`（卡-成员唯一绑定）。均为可空新列/新表，migrate+create_all 向前兼容，老库升级零丢失。
- **预设**：`services/templates_presets.py`（美业 beauty / 健身教培 fitness 两套：次卡+储值+家庭共享 + 建议档案字段）。
- **一键开店**：`services/templates.py`——`apply_preset`（按 presetKey 幂等、标记商户行业）、`onboarding_metrics`（复用 Merchant.createdAt 与 RedemptionRecord.createdAt 统计建店→首核销时长，无需新写入链路）。
- **家庭卡**：`issue_card` 按模板 allowFamily 置 shared；`my_cards`/`my_merchants` 含成员视角；`get_redeem_token`/`card_customer_detail` 放开家庭成员；`add_card_member`/`list_card_members` 幂等、成员须已建档。
- **路由**：`GET /api/templates/presets`、`POST /api/merchant/templates/apply`(issue_card)、`POST/GET /api/cards/<id>/members`、`GET /api/merchant/onboarding-metrics`(view_report)；`list_templates` 回传 industry/allowFamily/presetKey。
- **验证**：新增 `TestP2` 7 项；全量 **51/51 通过**（44+7）。

## 踩坑（已修复）
- `services/templates.py` 路由调用 `templates_svc.list_presets`，但该函数定义在 `templates_presets.py` → 在 `templates.py` 中 `from .templates_presets import list_presets` 重新导出解决。
- 测试误假设 `list_templates` 响应含 `total` 键（实际只回 `items`）→ 改用 `len(items)`。

## 待办提醒
- P1-4 前端（`card-counter-miniapp`）仍待另仓走查；V 类验证（SMB 访谈/试点 A-B）决定真实备案/存管对接投入。
- 下一步可推进：P3（微信极简小程序重构 / 透明定价页 / 团购·AI 跟随）或先启动 V-1 访谈。
