# 2026-09-11 工作日志

## P0-4 真实微信订阅消息接入（任务 #36–#41，已完成并验证）

**仓库**：`card-counter-commercial`（未改动 `card-counter-miniapp` / `card-counter-flask`）。
**背景**：P0-4 的提醒此前是占位实现（`notify.py::_send` 直接标 `sent`，不调微信）。

### 新增/改动
- **新增 `src/backend/services/wechat_mps.py`**：微信小程序接口底座。access_token 内存缓存（线程锁 +
  提前 300s 过期 + 失效自动刷新重试一次）；`subscribeMessage.send` 直连/代理双模式；
  errcode 归一化（43101→no_grant、40001/42001→token_invalid、47003→bad_param、45009→rate_limited）；
  模板字段适配（thing 截断 20 字、amount 分转元、date 补 `yyyy-MM-dd HH:mm`、缺字段不造空 value）。
- **新增模型 `SubscribeGrant`**（models.py）：一次性订阅授权台账，(openid, scene) 唯一，
  remaining/totalGranted/consumed。**这是"能不能发"的唯一依据**——一次性订阅每授权一次只能发一条。
- **重写 `services/notify.py::_deliver/_send`**：真实投递 + 五条状态分支（sent+delivered / demo /
  no_grant / failed）。**failed 与 no_grant 不扣额度**（避免白白消耗授权），按天去重、次日自动重试，
  `force=true` 可立即补发。`scan_reminders` 返回值新增 `delivered/skipped/noGrant/failed/channel`，
  `sent` 保持"新建提醒条数"旧语义（**兼容既有 3 个 P0-4 测试，未改任何老断言**）。
- **新增路由**：`GET /api/notifications/subscribe/config`（公开，只下发模板 ID 不下发 Secret）、
  `POST /api/notifications/subscribe`、`GET /api/notifications/grants`、
  `GET /api/merchant/notifications`（排查未送达，**不返回 openid**）、
  `POST /api/platform/notifications/scan`（全租户）。
- **前端**：商户端新增「提醒推送」Tab（通道状态 + 场景模板 + 扫描/强制补发 + 提醒记录）；
  顾客端新增「消息提醒」Tab（订阅额度 + 订阅入口 + 我的提醒）；平台「平台总览」新增全平台扫描。
  ⚠️ 踩坑：点击扫描后若整页重绘会清空结果面板 → 改为只刷新 `#n_records` 与 `#kpi .tmpl` 样式。
- **脚本**：`scripts/wx_stub_proxy.py`（本地微信桩，支持 43101 分支）、
  `scripts/verify_notify_ui.py`（浏览器端到端 + 截图，脚本内**幂等重置**通知/授权表）。
- **`run_tests.py`** 改为加载 4 个模块（core/concurrency/cross_tenant/notify_subscribe）。

### 验证结果
- 新用例 **28/28 通过**；全量 **163 项**（135 基线 + 28）。
- 浏览器端到端（本地桩代理模式）：未订阅时 生成2/送达0/未订阅2 → 顾客授权后各场景 1 条可发 →
  强制补发 送达2；平台全量扫描符合预期（额度耗尽 → 未订阅）；**0 JS 错误、0 服务端 4xx/5xx**。
- 截图存 `docs/`：提醒推送-未订阅 / 消息提醒-订阅授权 / 提醒推送-已送达 / 平台提醒-全量扫描（均 2026-09-11）。
- 接入文档：`docs/订阅消息接入-2026-09-11.md`（含小程序 `wx.requestSubscribeMessage` 可粘贴片段）。

### 待用户提供（阻塞真实上线，非代码问题）
1. 真实小程序 AppID/AppSecret + 后台申请的 3 个订阅消息模板 ID；
2. 定时任务（每日调 `POST /api/platform/notifications/scan`）；
3. 灰度环境用真凭据复跑一次验证（沙箱无外网，本次用本地桩验证链路）。

### 其它
- 演示库 `data/cards.db` 已被验证脚本重置（**已 gitignore**，不影响仓库）。

## 提交推送 + 部署状态核查（已完成）

- **已 commit + push**：`62350bd..9d7b61a`（25 文件 / +5341 −94），一次提交覆盖看板+登录+跨租户+订阅消息；
  origin/main 现已到 `9d7b61a`。提交前确认：无硬编码凭据、无 `*.db`/`logs`/缓存误入（`data/cards.db` 已在 .gitignore）。
- **部署就绪性已验证**：`requirements.txt` **无新增依赖**（`wechat_mps.py` 只用标准库 `urllib`）；用生产入口
  `wsgi.py`（`SEED=1` + 临时 DB）冷启动冒烟通过——`/api/health`=ok、`/`=200、
  `subscribe/config`=200、`platform/recon|merchant/dashboard|issuer/batches`=403、`wallet`=401（均为鉴权预期）。
- **⚠️ `cardcount-web` 未自动重新部署**：实测 push 后等 200s，线上仍是 `091ebde`（index.html 1176B、app.js 51866B、
  新路由 404）。结论：**该服务 push 不触发云端自动构建，需在云托管控制台手动「重新发布」**。
  排查手法：用字节数比对而非看内容——`curl -s -o /dev/null -w '%{size_download}' <域名>/web/app.js`
  （091ebde=51866 / 9d7b61a=89803）；index.html 1176 → 3513。
  另：演示服务会缩容到 0，首个请求返 503、重试即 200（冷启动），别把 503 误判为服务故障。
- **⚠️ 沙箱内长命令会被 SIGTERM（exit 137）且无任何输出**（包括 shell 自身的 echo）：
  跑 commercial 全量测试（~4.5min）必须用 `run_in_background=true` 并落日志文件。
  同理 `nohup ... &` 起的 gunicorn 会在本次 Bash 调用结束时被回收 → 也要用 `run_in_background=true`。

## 剩余工作量收口（#42–#49，已完成；三仓均**未推送**）

### 生产后端 `card-counter-flask`
- **A1** 回迁 `GET /api/cards/<cid>/customer-detail`（+`/voucher`）到 `wxcloudrun/commercial.py`/`commercial_views.py`；
  新增 `card_customer_detail()`（卡视图+退费规则+应退估算+合规徽标+凭证摘要）。集成测试 **44/44**。
- **A2** 新增 `wxcloudrun/concurrency.py`（**共享**进程内写锁）、`test_migration.py`、`run_tests.py`（4 套件一键跑）。
- **🔴 修复两个真实并发缺陷**（本轮最有价值发现）：`views.py` 的 `card_claim()` 与 `merchant_redeem()`
  **未加写锁**（`_write_locked` 只覆盖 commercial.py）。实测：同动态码 10 线程核销 → 8 条核销记录（应 1）；
  同领取码 10 线程 → 4 人领到（应 1）。加 `@write_locked` 后并发测试 12/12。
- **🔴 修复生产库迁移静默中断**：迁移循环用 `_conn.commit()`，但 **SQLAlchemy 1.4 legacy Connection 无 `.commit()`**
  （2.x 才有）→ AttributeError 被外层 except 吞掉 → 补列在第一条 ALTER 后**中断**（只补上 `legal_name`）。
  修法：新增 `_commit()`，有 `.commit()` 用之，否则回落 **`conn.connection.commit()`**（底层 DBAPI）；
  再发 `COMMIT` SQL 会报 "cannot commit - no transaction is active"（DDL 已 autocommit）。22 处调用点全局替换。

### 顾客端小程序 `card-counter-miniapp`（B 系列）
- 卡详情页改消费 `customer-detail` 单端点 + 降级链；tabBar 5→3（P3-1 极简）；
  修 3 处 `switchTab` 指向非 tab 页的隐藏「点不动」bug；版本号 2.7.2。

### 企业端小程序 `card-counter-merchant-miniapp`（C1）+ commercial 后端
- 新增 `GET /api/merchant/cards/<id>/detail`（卡视图+全量记录+凭证摘要，需 view_report，跨商户 404）。
- **🔴 收紧 `GET /api/cards/<id>/records` 鉴权**：此前**无任何鉴权**，知道 cardId 即可读任意卡审计链。
- **透传 `/api/merchant/redeem` 的 `idempotencyKey`**：此前被忽略 → 并发请求共用默认键
  `redeem:manual:{card}:{staff}:{ts//5}`（5s 窗口）而被幂等缓存短路，**掩盖超卖**（压测表现为 40 全 200 但只扣 1 次）。
- 企业端 cards 页重写：行可点击展开详情 + 逐笔核销 + 「导出凭证 PDF/Excel」(`wx.downloadFile`+`openDocument`)
  + 异常核销告警面板（查看卡片/标记已处理）；`utils/api.js` 新增 `download()`。
- commercial 全量 **171/171 通过**（163 + 8 新用例）。

### D1 MySQL 多 worker 并发压测
- 新增 `scripts/load_redeem_http.py`（**纯标准库** HTTP 并发压测，可打本地也可打线上）。
- 沙箱**无 MySQL/docker/pymysql/外网** → MySQL 正例未跑（脚本+步骤已就绪，见 `docs/多worker并发压测-E3.md`）。
- **本地实测（关键结论）**：
  - 单 worker + SQLite：次数卡 40 并发/容量 10 → 10 成功 ✅；储值卡 80 并发/容量 20 → 20 成功、余额 0 ✅
  - **4 worker + SQLite：次数卡 40 并发/容量 10 → 13 成功（超卖 3 次）❌；
    储值卡 → 23 成功、余额 **-300 分**（超支 ¥3）❌**
  → 用可复现数据证明 `gunicorn.conf.py` 的 `workers=1` 是**正确性约束**，放开 worker 必须切 MySQL 行锁。
- **🔴 新发现部署硬约束**：`gunicorn` 多 worker 时 **`SEED` 必须=0**，否则 N 个 worker 并发 seed 撞
  `UNIQUE constraint failed: merchant.id` → **worker 全部 boot 失败**。正确做法：先单进程 `SEED=1` 种一次，再 `SEED=0` 起多 worker。
- 为跑压测临时装进 `.venv`：`PyMySQL==1.1.1`、`gunicorn==26.0.0`（`requirements.txt` 本就有 pymysql/gunicorn）。

### 收口文档
- `docs/剩余工作量收口-2026-09-11.md`：A 本轮已完成 / B 必须用户出手（#1 小程序提审 2.7.2、#5 企业端新 AppID、
  #7 合规备案·存管·支付资质、#10 V 系列访谈/AB/价格）/ C 未推送改动一览 / D 待验证与风险 / E 已关闭重复项。
- 更新 `docs/多worker并发压测-E3.md`（实测表 + 两条部署硬约束）、`docs/小程序前端改造-B.md`（新增 C 节）。
- ⚠️ **`card-counter-miniapp` 不是独立仓库**：其 git 根是 **`/home/wang/wk`**（提交时只 `git add code/card-counter-miniapp`）。

---

## 执行发布（2026-09-11 09:20 起，用户指令「请直接发布」）

### 推送（均成功）
- `card-counter-flask`：`dd50655 → f43a4d3`（push main → 应触发 flask-z9hh 自动部署）。
- `card-counter-commercial`：`9d7b61a → bf9178b`（**不自动部署**，需云托管控制台手动重新发布）。
- `card-counter-merchant-miniapp`：`f9209a7 → 9dc67ec`（推 GitHub；无 AppID 无法上传微信）。
- `card-counter-miniapp`：改动提交到 `/home/wang/wk`（branch `python`，remote=base-table.git）本地 `c102b308`，**未推送**（该 remote 是个人笔记仓，含 5 个未推送 obsidian 提交）。

### 小程序 2.7.2 上传微信（成功）
- 首次报 `-10008 invalid ip: 2408:820c:6809:2bc1::e6a`（IPv6 出口不在白名单）。
- **根因（重要）**：本机是**分流出口**——国内 IPv4 → `112.65.121.227`（白名单内，用 `curl -4 http://ip.3322.net/` 验证）；
  国外 IPv4 → `141.11.22.43`（**勿用 ifconfig.me 判白名单**）；IPv6 → `2408:820c:...`（未白名单，Node 优先选它）。
- `NODE_OPTIONS=--dns-result-order=ipv4first` **无效**；`--no-network-family-autoselection` 无效；
  本地 CONNECT 代理 + `HTTPS_PROXY` 也无效（流量仍出 IPv6）。**只有覆盖 `dns.lookup` 强制 `family:4` 才有效。**
- ⚠️ 即便强制 IPv4，该路径仍有**约 50% 抖动**（同样命令一次成功一次 `-10008`）→ 必须重试。
- 已把「强制 IPv4 DNS + 8 次重试」内置进 `card-counter-miniapp/scripts/upload_ci.js`（`UPLOAD_ATTEMPTS` 可调）；
  踩坑：miniprogram-ci 的错误对象 `.message` 常为空，判断可重试要读 `e.errMsg`/`String(e)`。
- 结果：**2.7.2 已上传成功**（`subPackageInfo size 185008`），待用户在 MP 后台提交审核。

### 部署验证（发布后 24 分钟）
- ⚠️ **生产后端 flask-z9hh 未换版**：已 push `f43a4d3`，但 `/api/cards/1/customer-detail` 仍 **404**（旧代码），
  对照 `/api/cards/1/refund-estimate`=401（服务在线）→ **本次 GitHub 自动部署没生效或构建失败**，需用户到云托管控制台看构建记录/手动重新构建发布。
- ⚠️ **cardcount-web 仍是旧版 `091ebde`**（`web/app.js`=51866B；新路由 `/api/notifications/subscribe/config`、
  `/api/merchant/cards/1/detail` 均 404）→ 需控制台手动重新发布。
- 已更新 `docs/剩余工作量收口-2026-09-11.md`（新增 §F 发布执行结果）；精简重写 `MEMORY.md`（14.2KB→7.6KB，系统提示超限截断）。


## 领卡链路梳理与端到端实测（11:06-11:10）
- 问题：「发卡后，顾客端怎么把卡加进来」。结论 4 条路径：①领取码（主路径，`pages/merchant/index` 发卡→`issueCode`→`pages/card/claim` `POST /api/cards/claim`）；②线上买卡（`pages/order/buy` 下单+pay→`pay_order()` 直发）；③团购券（`pages/groupbuy/redeem`）；④卡密 12 位（后端 `POST /api/redeem`+`cardkey_svc.redeem()` 完备，但小程序/H5 均**无 UI 入口**，缺口）。
- ⚠️ UX 断层：首页 Tab「卡包」的「+添加次卡」→`pages/quota/quota` 是**本地自建次卡**（/api/quotas），与商户发的云端卡（/api/cards，入口在「我的」→「我的卡包」）是两套数据；H5 index.html 也无领卡入口。
- 端到端实测（8091 本地生产后端）：建卡种→发卡拿码 BNQZXCUU→顾客 claim→卡包 4→5 张（#23 active 5次 有效期30天）→他人同码 400 拒绝。验证脚本 `/home/wang/wk/tmp/e2e_card.py`。
- 新坑：本地签 JWT 用 `datetime.utcnow().timestamp()` 在 GMT+8 机器会少 8h → exp 已过期（「登录已过期」401）；必须用 `time.time()`。服务端 generate_token 同样写法，容器 TZ=UTC 才没问题——若将来容器改时区会踩雷。
- 商户端发卡页文案：「把领取码发给顾客，顾客在『领卡』页输入即可绑定」。

## 网页端商业功能补全（21:13-21:40）
- 用户要求「继续补全，实现满足商业版本要求功能」（网页端顾客侧）。
- 新增后端 `GET /api/shop/templates`（顾客可浏览在售卡种，购买前明示 refundRule+refundRuleType——合规硬要求；?merchantId 可选，演示回落商户1）。
- web/app.js 新增学生 tabs：「在线购卡」tabShop（卡种卡片含退费规则明文→立即购买→POST /api/orders→pay→发卡入包）、「我的订单」tabOrders（列表+待支付可「去支付」）；「我的次卡」每张 active 卡加「退费估算」按钮→openModal 展示 paid/used/estimate/规则类型+明文。
- 端到端实测（8092 演示服务）：下单订单#1 108000分→支付→发卡#5 active→订单列表 paid→退费估算未用全退 108000 分。全绿。
- 新测试 test_shop_templates_lists_active_with_refund_rule（TestP3Order，含 disabled 模板不上架断言）；atomic 从 backend.services 导入（不是 backend.db）。
- 坑：旧 gunicorn 在隔离 pid namespace 杀不到（pkill/TaskStop 无 id）→ 换端口 8092 起新版即可；web/app.js 是静态读盘，改完无需重启也生效（但 py 代码要重启）。
- 全量回归 184 项（183+1）后台跑 run_all4.log。
