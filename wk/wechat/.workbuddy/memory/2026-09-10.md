# 2026-09-10

## 设计任务：科技博主「FDE」头像
- Ardot 画布 MCP 工具本次会话不可用（mcp__ardot__create_design 不存在），改为本地可交付方案。
- 交付 1：几何字母组合 SVG 头像 `/home/wang/wk/wk/wechat/fde-avatar.svg`（1080×1080，深靛蓝+霓虹青渐变，FDE 块面字体 + 光圈徽章，含 @FDE / 科技博主 文案）。矢量可无损缩放，便于导出 PNG。
- 交付 2：AI 生成插画头像 `/home/wang/wk/wk/wechat/generated-images/Stylized_minimal_flat_illustra_2026-09-10T14-26-06.png`（1024×1024，戴眼镜+耳机的科技博主扁平插画风，消耗约 5–10 credits）。
- 教训：画布不可用时优先产出本地 SVG/图片交付物，而非卡在 create_design。

## 商业化继续开发：商户经营看板 + 平台总览（card-counter-commercial）
- 范围：**仅改 `card-counter-commercial`**，不动 `card-counter-miniapp`（顾客端小程序）与 `card-counter-flask`（生产网页后端）。
- 新增 `src/backend/services/dashboard.py`：`merchant_dashboard()` / `platform_overview()` / `compliance_summary()`（纯读取，不改资金链路；核销趋势 Python 侧按日分桶，方言无关）。路由：`GET /api/merchant/dashboard?days=14`（view_report）、`GET /api/platform/overview`（平台超管）。
- 前端 `web/app.js`：owner 新增首个 Tab「经营看板」（KPI 网格+14日核销趋势条+RFM 分层+风险/到期+热销产品）；platform 新增首个 Tab「平台总览」。`web/styles.css` 加 `.kpi-grid/.kpi/.bars/.segbar`。
- 测试 `TestDashboard` 3 项；全量 **103/103 通过**（98 基线 + 看板 3 + 并发 2）。真实 HTTP 冒烟（PORT=8091 隔离库）通过。
- 文档：`wechat/docs/商户经营看板-验收说明-2026-09-10.md`。
- ⚠️ 环境踩坑：`/tmp` 是**仅 10MB tmpfs**，测试建临时 SQLite 报 `database or disk is full`（非代码问题）→ **跑测试前 `export TMPDIR=/home/wang/wk/tmp`**（该大盘目录）。系统 Python 受 PEP 668 → 依赖装仓内 venv（Flask 3.1.3 / SQLAlchemy 2.0.51）。
- 遗留：未部署到线上 `cardcount-web`（非 push 自动部署，需控制台手动重新部署）；改动未 commit/push。

## 登录验证功能：用户名密码 + 微信公众号（card-counter-commercial）
- 范围同上：**仅改 `card-counter-commercial`**。
- 现况澄清：用户名密码登录**后端早已完备**（`services/auth.py`：PBKDF2 + 不透明 Bearer + 可吊销；`UserAccount`/`AuthToken`；seed 账号 `admin1`/`clerk1`/`platform`）。本次真正缺的是 **① 微信公众号登录 ② 前端登录闸门（基于 token 验证）**。
- 新增 `src/backend/services/wechat_auth.py`：公众号网页授权 OAuth2（authorizeUrl + HMAC `ts.sig` state 防 CSRF/10min 时效 + code→openid 换取的纯 stdlib urllib 实现）；未配置 env 时降级「演示模式」。
- `auth.py` 抽出 `issue_token()`；新增 `wechat_login()`（openid find-or-create Customer + UserAccount(actorType=customer)，幂等）；`actor_view` 支持 customer。**actorType 由 platform_admin/staff 扩展为 +customer**。
- 路由新增：`GET /api/auth/wechat/config`、`POST /api/auth/wechat/authorize`、`GET|POST /api/auth/wechat/callback`、`POST /api/auth/wechat/demo`、`GET /api/auth/demo-accounts`。`_customer_id/_customer_openid` 增加 **Bearer 回退**（无演示头也能解析顾客身份）。
- 前端 `web/index.html|app.js|styles.css`：全屏登录闸门（双 Tab：账号密码 / 微信），token 存 `localStorage["cc.auth.v1"]`，`api()` 注入 `Authorization`，401 自动回闸门；**角色由账号推导**（platform_admin→platform · staff(isOwner)→owner · staff→clerk · customer→student）；保留「演示模式进入」不破坏旧 demo；favicon 改内联 SVG 消 404。
- 生产启用真实公众号只需设 env：`WECHAT_OAUTH_APPID/SECRET/REDIRECT/SCOPE`，生产建议 `WECHAT_OAUTH_ALLOW_DEMO=0`（代码零改动）。
- 测试：`TestWechatAuth` 10 项；全量 **113/113 通过**（103 + 10）。真实 HTTP 冒烟 `scripts/smoke_login.py`；浏览器端到端 `scripts/verify_login_browser.py`（Playwright + 系统 `/usr/bin/chromium`，**JS 错误 0 / 4xx-5xx 0**）。
- ⚠️ Playwright 自带 chromium 版本不匹配（缺 `chromium_headless_shell-1223`）→ 用 `launch(executable_path="/usr/bin/chromium")`；且 `wait_for_selector` 默认 `state="visible"`，等隐藏元素须显式 `state="hidden"`。
- 文档：`wechat/docs/登录验证-验收说明-2026-09-10.md`。遗留同看板：未部署、未 commit。

## 跨租户集成：卡密分发 + 统一账本 + 同步/对账（card-counter-commercial）
- 依据工作区 `tasks/次卡管理 - 跨租户集成.md`（跨公司 SaaS 卡密分发 + 多租户客户端自动同步规划）。范围：**仅改 `card-counter-commercial`**，不动小程序与生产网页后端。定位 = 规划文档 Phase 0+1 / 验证里程碑 M1+M2 的可运行切片。
- 模型（models.py 追加 8 表）：`card_batch` / `card_key` / `card_binding` / `ledger_entry` / `spend_event` / `sync_cursor` / `recon_diff` / `risk_event`。靠既有 `migrate()`+`create_all()` 向前兼容。
- 服务（新增 4 模块）：`services/cardkey.py`（批量生成/兑换/消费/报表/风控）、`services/ledger.py`（复式记账+全局单调 seq+余额快照）、`services/sync.py`（changes since=seq + 设备游标 ack）、`services/recon.py`（三方对账 + 差异单 + 一致性率）。
- 路由 15 个：发卡方 `/api/issuer/batches(+/<id>, /report, /callback)`；用户端 `/api/redeem`、`/api/wallet`、`/api/spend`、`/api/changes`、`/api/sync/ack`；平台 `/api/platform/recon(+/run, /<id>/resolve)`、`/api/platform/ledger`。
- 前端：owner 新 Tab「卡密中心」（批次生成+明文仅一次展示+CSV 下载+核销报表+批次列表）；platform 新 Tab「对账中心」（三方 KPI+一键对账+差异单结单）。`styles.css` 加 `.amt-neg/.amt-pos`。
- 测试：新增 `backend/tests/test_cross_tenant.py` 22 项；全量 **135/135 通过**（113 + 22）。覆盖幂等/状态机/借贷平衡/seq 单调/20 线程抢同卡仅 1 成功/30 线程消费不超扣/增量同步/对账检出与结单/撞库风控/权限隔离/明文不落库。
- 浏览器端到端：`scripts/verify_cross_tenant_ui.py`（Playwright + /usr/bin/chromium）→ 生成 3 张卡密、切平台角色一键对账 100%、**JS 错误 0 / 4xx-5xx 0**。
- 三处修复：①撞库风控事件被主事务回滚 → `/api/redeem` 手工 try/except 在回滚后独立事务补记；②对账对未兑换卡误报 → 卡级校验仅限已兑换卡；③安全自查：初版 cardNo 即兑换码明文入库 → 重构为**展示编号 ≠ 兑换码，码仅哈希**。
- 文档：`wechat/docs/跨租户集成-验收说明-2026-09-10.md`（含逐条对照文档 §5.1 验证目标）。遗留：未部署、未 commit（同前两批改动）。
