# 2026-08-23 工作日志

## 续执行下一步计划（A → C → D → B）全部完成
仓库：`card-counter-commercial`，全量测试 **67/67 通过**（基线 54 + 新增 13：RFM×3 / Order×4 / Refund×3 / GroupBuy×3）。

### A · P3-4 RFM 客群分层（纯读取）
- 新 `services/rfm.py`：R/F/M 分位打分 1-5 + 业务标签；`compute_segments` / `segment_summary`。
- 路由 `GET /api/merchant/segments`（`?summary=1` 汇总），view_report 权限。
- 测试 `TestP3RFM`×3。

### C · 订单/支付闭环
- `Order` 加 `refTemplateId`（可空，migrate 补列）。
- `issue_card` 支持 `customer_id`+`customer_openid` 直发（immediate 绑 owner，复用幂等/审计/聚合重算）。
- 新 `services/order.py`：`create_order`(buy_card/renew) / `pay_order`(PaymentRecord.success+Order.paid→issue_card/extend_card,重复支付幂等) / `my_orders`。
- 路由 `POST /api/orders`、`POST /api/orders/<id>/pay`、`GET /api/orders/mine`（顾客端）。
- 测试 `TestP3Order`×4。

### D · 退费规则结构化 + 应退计算
- `CardTemplate` 加 `refundRuleType`/`deductPerUseCents`/`maxRefundCents`（可空）。
- 新 `services/refund.py`：`estimate_refund`（paid 优先 Order.paidAmount 累加，无订单回退模板售价；储值卡退余额；maxRefundCents 封顶）。
- 路由 `GET /api/cards/<id>/refund-estimate`（本人或 view_report）；`card_customer_detail` 聚合透出 `refundEstimate`。
- seed：tpl10=prorata_by_used+deductPerUseCents，tpl_yoga/tpl_sv=unused_full。
- 测试 `TestP3Refund`×3。

### B · 团购核销落私域（占位）
- 新 `GroupBuyVoucher` 模型 + `services/groupbuy.py`：`redeem_voucher`（锁行+幂等+审计+直发 issue_card）；保留 `_fetch_voucher_from_platform` 真实平台接入点（TODO，待美团/抖音凭证）。
- 路由 `POST /api/group-buy/redeem`（顾客端）；seed 演示券 `GB-DEMO-1001`（美团，绑定 tpl10）。
- 测试 `TestP3GroupBuy`×3。

## 踩坑
- seed.py 内导入 GroupBuyVoucher 必须用 `from .models import`（同包），`from ..models` 会超顶层包报错（ImportError: attempted relative import beyond top-level package）。
- `card_customer_detail` 返回 `{"card": view, ...}`，expireDate 在 `card` 子对象内，测试读 `body["card"]["expireDate"]` 而非顶层。

## 文档更新
- `tasks/开发待办事项-2026-08-20.md`：P3-3/P3-4 标记后端落地，新增 C/D 闭环小节，全量计数更新为 67/67。
- `MEMORY.md`：更新服务清单、下一步方案（A/C/D/B 已完成，剩 F/E/G）。
