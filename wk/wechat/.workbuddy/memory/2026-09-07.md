# 2026-09-07 工作日志

## 启动 B/E/F 开发 + 验证
- 清理 MEMORY.md（超限截断）：压缩为 6 段（部署/代码库/约定/UX原则/踩坑/并发约束）。
- **E（质量与性能）**：
  - E1 基线：commercial 全量 **98/98 通过**。
  - E2 并发用例：新增 `card-counter-commercial/src/backend/tests/test_concurrency.py`（线程级），验证 10 次卡 20 线程→10 成功/10 拦截、¥2000 储值卡 30 线程→20 成功/10 拦截；**通过**。全量 **100/100**。
  - E3 多 worker 压测：新增 `test_concurrency_mysql.py`（multiprocessing 模拟 gunicorn workers，依赖 MySQL `FOR UPDATE` 行锁），无 MySQL 时整类 skip（已验证 skip 正确）。文档 `docs/多worker并发压测-E3.md`（含 docker-compose + gunicorn + 唯一 idempotencyKey 提醒）。
- **B（小程序前端）**：
  - B1 走查发现：prod 后端 flask-z9hh **缺** `/api/cards/<id>/customer-detail` 与 `/api/cards/<id>/refund-estimate`（后者被 detail.js 静默 404）；Card 仅次卡模型、checkin 未关联 card → 单卡核销明细 prod 不可查。
  - B2 实现：卡详情页加「我的核销记录」区块（best-effort 拉 `/api/checkins`，失败不阻塞，沿用登录可选原则）。改 `pages/card/detail/{detail.js,detail.wxml,detail.wxss}`，`node --check` 通过。
  - B3 极简页流方案已写入 `docs/小程序前端改造-B.md`（待渐进实施）。
  - 新增任务 #21：后端回迁 customer-detail + 卡→核销关联到 card-counter-flask（P1-4 完整闭环，需 GitHub push）。
- **F（市场验证）**：产出 3 份验证框架文档 —— `docs/SMB访谈提纲-F1.md`、`docs/试点门店AB测试-F2.md`、`docs/价格敏感度测试-F3.md`。

## 注意
- B2 miniapp 改动未提交/未上传；v2.7.1 已含取消强制登录。B2 要生效需重新 `upload_ci` + 提交审核。
- 本机无 MySQL，E3 仅验证 skip 路径；真正多 worker 压测需用户在 MySQL 环境跑。
