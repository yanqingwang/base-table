# 2026-09-05 工作日志

## 次卡管家 v2.7.0 发布继续

- 复核两个平台阻塞点当前状态：
  - 小程序上传：微信侧实际见到出口 IP `183.192.126.7`（NAT 动态，曾见 112.65.121.227 / IPv6 2408:820c:…），仍报 `errCode -10008 invalid ip`。本机出口不稳定 → 加单 IP 白名单脆弱，建议固定 IP 机器上传。
  - 后端部署：`tcb cloudrun deploy` 报 `云托管资源未开通`（环境 cardcount-d4gjfjexz3097d803 / ap-shanghai 容器型云托管未激活）。
- 发现并修正记忆错误：生产后端 `card-counter-flask` 的 A/C/D/B 移植此前**并未提交**（`2e8c853b` 是 commercial 演示仓提交，prod 后端无此提交）。当前 prod 后端有未提交 `wxcloudrun/commercial.py`、`commercial_views.py`、修改 `__init__.py`(+77)/`model.py`(+356)、未跟踪 `cloudbaserc.json` + 两个测试文件。
- 跑通测试（系统 Flask 3.1.3 兼容）：集成 `test_commercial_integration.py` 31/31、并发 `test_commercial_concurrency.py` 7/7 通过。
- 提交并推送 prod 后端移植：`d91d4fe`（main, 已推 origin/main）。小程序 `f3f05a3e`（v2.7.0）已在本地提交、工作区干净，但属 `/home/wang/wk` 单仓、含大量无关未提交改动，暂不 push 整个单仓。
- 产出发布建议文档：`wechat/docs/发布建议-2026-09-05.md`（含两阻塞点处置 + Runbook 7 步）。
- 修正 MEMORY.md 发布状态（prod 后端提交实为 `d91d4fe`；部署报错确认为"云托管资源未开通"）。

## 14:03 复检（用户称白名单已加）
- 小程序上传重试仍报 `invalid ip: 183.192.126.7`（出口 IP 稳定未变）。→ 用户所加白名单极可能是旧 IP（112.65.121.227 / IPv6 2408:820c:…），当前 NAT 出口已变为 183.192.126.7。需白名单加入精确当前 IP `183.192.126.7`（appid wx9c5974ab24d057c3）。
- 后端 `tcb cloudrun deploy` 仍 `云托管资源未开通` → 容器型云托管仍未激活，需控制台开通。

## 待用户操作
1. 小程序上传 IP 白名单：必须把**精确当前**出口 `183.192.126.7` 加入（appid wx9c5974ab24d057c3）；或改用固定 IP 机器上传（推荐，避免 NAT 漂移）。
2. CloudBase 控制台开通环境 cardcount-d4gjfjexz3097d803 的容器型云托管。
完成后我推进上传重试 + 后端部署 + 打 tag v2.7.0。

## 14:10–14:25 发布重大进展（v2.7.0 双端就绪）
- ✅ 小程序上传 v2.7.0 **成功**（用户把当前出口 `183.192.126.7` 加入白名单后）：`✅ 上传成功`，主包 177KB。
- 🔑 **关键发现：生产后端真实发布链路 = 微信云托管 GitHub 自动部署**（非 tcb/CloudBase cardcount 环境）：
  - 用默认 profile chromium(CDP 9222) 查 cloud.weixin.qq.com/cloudrun/service：环境 **prod**(envId prod-d5gm4a2q00a7f9209) / 服务 **flask-z9hh 服务正常** / 发布方式=GitHub 仓库 `yanqingwang/card-counter-flask`，push main 自动构建部署。
  - 线上版本 **flask-z9hh-057**，版本备注=commit `dd50655`，发布时间 14:10:31（=push 时刻）→ d91d4fe(A/C/D/B)+dd50655 **已自动上线**。
  - 公网域名 https://flask-z9hh-281177-5-1453124923.sh.run.tcloudbase.com；健康检查 GET /=200，/api/merchant/stores、/api/my/cards 返回结构化 401 JSON（新路由存活）。
  - 小程序 `utils/config.js` develop/trial/release 三环境均指向该 webBase → 无需改。
- ⚠️ 教训：`tcb cloudrun deploy` + cardcount-d4gjfjexz3097d803 整套是**错误路径**（那是另一无关测试环境；"云托管资源未开通"不再相关）。prod 部署=推 GitHub main。
- ✅ git tag v2.7.0：后端推 origin（at dd50655）；小程序本地 tag（at f3f05a3e，单仓不推）。
- ⏳ 剩余：小程序**提交审核**（mp.weixin.qq.com 登录超时，需用户扫码登录后手动提交；版本管理→开发版本 2.7.0→提交审核）→ 审核通过后发布。
- 📌 用户要求将今日澄清作为关键信息入长期记忆 → MEMORY.md 已新增置顶「⚠️ 重要澄清（2026-09-05）」小节（flask-z9hh=服务名 vs cardcount-*=无关测试环境；生产=微信云托管 prod/环境 prod-d5gm4a2q00a7f9209，push GitHub main 即自动部署；勿用 tcb）。
- 🧪 商户端「发卡」功能已基于 `card-counter-commercial` 网页双端 Demo 实测：CDP 驱动 chromium 跑完 5 个 TC（客户领取/立即生效/顾客领卡/核销员越权 403/销卡）全部通过；产出报告 `wechat/docs/商户端发卡测试报告-2026-09-05.md` + 6 张截图归档到 `wechat/docs/screenshots/商户端发卡测试-2026-09-05/`。演示服务（PORT=8090，临时 SQLite）已关闭。

## 15:10–15:35 排课/预约/签到 全链路补齐 + 验证
- 经用户确认口径：自动排课=「每时段多节切分」；预约即确认、到店手动签到（不做商户逐条审核）。
- 改动（card-counter-commercial）：
  - models.ScheduleRule + `perSessionMinutes`/`breakMinutes`（可空，None=旧整段行为）。
  - schedule.py：create_schedule_rule 落库课长/休息；generate_sessions 多节切分（每日≤10、逐节 check_conflicts、节假/冲突跳过计数）→ 返回 `{ruleId, created, skippedConflicts, skippedHolidays}`（⚠️ 返回值由列表改 dict，仅路由/seed 消费）；新增 list_schedule_rules。
  - booking.py：候补去重(409) + my_waitlists/cancel_waitlist（越权 Forbidden 403）。
  - routes：GET /api/merchant/schedule-rules；GET /api/my/waitlists；POST /api/my/waitlists/<id>/cancel；generate 幂等键 key 修正（`idempotency_key`）。
  - web/app.js：排课 Tab 顶部「自动排课向导+规则列表」（再次生成补缺）；学员课次预约/我的预约 接入候补自助。
- 验证：全量测试 **77/77**（+8：TestScheduleV2Auto×4 / TestBookingWaitlistAttendance×4）；演示服务(8090 新代码+全新库) UI 真机点向导 生成28节/冲突跳过2节；报告 `wechat/docs/排课预约开发验证-2026-09-05.md` + 截图 screenshots/排课预约开发验证-2026-09-05/。
- ⚠️ 坑：BizError(code,msg) 默认 status=400 → 权限拒绝要显式 Forbidden/status=403；`/api/merchant/sessions?to=<当天>` 会把当天 00:00 后课次过滤掉（需 to=次日）。
- 演示服务保持运行：http://127.0.0.1:8090（pid 见 /tmp/cc_demo.pid，DB /tmp/cc_demo.db）。

## 15:40–16:05 独立企业端小程序 MVP（card-counter-merchant-miniapp）
- 新建 `code/card-counter-merchant-miniapp`（独立仓，与顾客端分开）：tabBar 工作台/发卡/核销/课次/我的 + login(演示身份店长staff1/核销员staff3)+cards+roster；utils/config(dev=http://127.0.0.1:8090)+api(X-Merchant-Id/X-Staff-Id+幂等)；8 页 .js/.wxml/.json 齐。
- 复用演示后端 `/api/merchant/*` 契约（发卡/销卡/延期/redeem/课次/名单/候补递补）。
- 自验：JS/JSON/页面一致 ✅；14 接口与 demo 路由逐一对齐 ✅；真实联调 8/8（发卡 active、坏核销码400、核销员发卡403）✅。
- 📄 `wechat/docs/企业端小程序-MVP验证与发布清单-2026-09-05.md`。
- ⏸️ 真实发布被 2 个闸门卡住（非代码）：①需要**新 AppID+private.key+上传白名单**（独立小程序；勿用顾客端 AppID 覆盖）；②生产后端需先做「员工微信登录 openid→staff」（现在 prod 是 JWT，且小程序只能拿 openid）。跨企业顾客分组(my/merchants→prod + 顾客端 UI)排 v2.8.0。

## 16:47–17:00 浏览器登录的是「小游戏测试号」；v3 多租户方案定稿
- 浏览器登录的后台其实是 **WeChat 测试号**（AppID `wxf46fadd1ddaa167a`，AppSecret 可见）：测试号无上传私钥/IP白名单/提审发布，仅可联调。已把 merchant miniapp `project.config.json` appid 改为该测试号并在 README 注明；用户将另行注册正式小程序。
- 产品负责人拍板 4 项企业端决策：D1 平台(租户)层入口=**Web 后台**；D2 企业内=**授权管理员(多)→门店→店长/店员**三级、单店管理员=店长；D3 跨店池=**混合**（默认全店共享一池，部分产品门店专属）；D4 **核销/预约校验门店范围**+顾客端显示可用门店。
- 📄 方案固化：`wechat/docs/企业端多租户门店与跨店消费-v3方案-2026-09-05.md`（分层/角色权限/模型增量/接口草图/M1-M3）。
- 下一步：确认后在 card-counter-commercial 演示仓实施 **M1**（三级角色+storeScope(shared/single/storeOnly)+按店校验+usableStores+/api/merchant/tree）+ 单测；M2=正式企业端小程序发布；M3=平台 Web 后台。

## 17:20–17:40 v3 M1 完成（网页版优先实现，83/83）
- 实现（demo 仓）：models 加 `Merchant.admins`/`MerchantStore.managerStaffId`/`Staff.storeId`/`CardTemplate.storeScope+allowedStoreIds+poolMode`；新 `services/store_scope.py`；发卡/核销/预约带 storeId 并按范围校验；`GET /api/merchant/tree`；顾客端 cards/mine 带 usableStoreIds/Names；前端加「组织与门店」Tab、发卡/核销门店选择、我的次卡显示可用门店。
- 测试 **83/83**（+TestTenantStoreScope×6：跨店核销409/本店200、shared 全店跨店、allowed 限店、single 卡约他店课409、未绑店发卡400、mine usable、merchant/tree）。
- 冒烟：tree admins[1]/店1店长小林；跨店核销 409「卡#4不可在门店#2使用（模板范围：仅发卡门店）」；本店200 deduct1；mine usable=[1]「静安旗舰店」。
- 📄 `wechat/docs/企业端v3-M1多门店与跨店范围-验收说明-2026-09-05.md` + 截图 07_v3_org_stores_tab.png。

## 17:45–18:00 M1 收尾（店员管理，85/85）+ 演示部署就绪
- 补 `POST /api/merchant/staff`（kind=clerk/front/store_manager 建店员+归属门店+权限角色）与 `PATCH /api/merchant/staff/<id>`（调店 storeId/设店长 managerOfStore）；网页「组织与门店」加建店员表单；核销员越权调店员接口 403。
- 测试 **85/85**（+2：建店员并设店长两阶段、越权 403）。冒烟：建店员小陈→设为浦东二店店长→tree manager 生效。
- 网页版演示运行于 **http://127.0.0.1:8090**（全新库 /tmp/cc_demo.db，pid /tmp/cc_demo.pid）；生产部署建议另起微信云托管服务(git 源)或自持服务器跑 gunicorn（见 DEPLOY.md）。

## 18:20 cardcount-web 部署完成（✅ 已上线，无需用户手动操作）
- **线上版本 `cardcount-web-002`**（18:16:48 发布，服务正常 1 实例；001 是 registry 瞬时错误失败）。
- **公网域名 `https://cardcount-web-281177-5-1453124923.sh.run.tcloudbase.com`**（同 flask-z9hh 的 env prod 域名段）。
- 验证：`/`=200（标题「次卡管理 · 商业化 SaaS 演示」）；`/api/health` ok；`/api/merchant/cards` 返回 seed 卡；`/api/merchant/tree` 返回 M1 组织树（admins[1]/店长小林/前台小王）；`card-templates` 带 storeScope/poolMode（v3 M1 schema 在线）。代码=GitHub `091ebde`（含 M1 全功能）。
- ⚠️ 遗留：本地 commit `e7b4a12`(chore deploy 默认配置) 因 github.com 不可达未推（仅默认值加固，不阻塞）；线上 DB=容器内默认库（重启重置）→ 正式商用需 MySQL + 关 SEED。

## 17:50–18:20 env prod 部署 cardcount-web（进度 + 待用户收尾 5 步）
- **GitHub 私有仓** yanqingwang/card-counter-commercial 已建并 push（commit `091ebde`），含 Dockerfile/gunicorn.conf.py/wsgi.py/requirements.txt/src/+/web/，可由云托管自动识别容器构建。
- **控制台** env prod-d5gm4a2q00a7f9209 下服务 `cardcount-web` 已建（公网+内网访问），跳转部署页「步骤 1 选择代码」。⚠️ 控制台选择仓库弹窗的 CDP 自动化卡住，剩余 5 步（仓库选 yanqingwang/card-counter-commercial / 分支 main / 端口 80 / 环境变量 DB_URL=sqlite:////tmp/cardcount_web.db & SEED=1 & PORT=80 / 发布）交由用户手动操作；runbook 见 `wechat/docs/商业网页部署到云托管-runbook-2026-09-05.md` + 截图 `云托管-选择代码-2026-09-05.png`。
- 教训：env prod 控制台不在 tcb CLI 账号下（早期 tcb cloudrun list -e prod... 报 env not found），无法用 tcb 部署；只能走 cloud.weixin.qq.com 浏览器控制台。
- 演示服务(8090, /tmp/cc_demo.db) 运行中，含演示二店「浦东二店」与模板「限店卡」。
- 遗留：门店/店员管理 UI(接口预置)、M2 等新 AppID、平台 Web 后台(M3)。

## 19:30–20:10 M3 平台租户层管理开发完成（89/89）+ 推送；云端待手动发布
- **M3 实现**（demo 仓，commit `c041857` 已推 GitHub main）：
  - `Merchant.status`(active/suspended, migrate 补列) + 新表 `PlatformAdmin`(seed id=1 演示超管)
  - 新 `services/platform.py`：list_merchants / open_merchant(自动建企业+角色「企业管理员」+授权管理员 Staff.isOwner 并写 Merchant.admins，幂等) / set_merchant_status(审计)
  - 新路由 `/api/platform/merchants`(GET/POST) + `/api/platform/merchants/<id>/status`(PATCH)，鉴权 `X-Platform-Admin` 头 `_require_platform`（与商户/顾客完全隔离）
  - `ensure_permission` 加固：企业 status=suspended → 其员工一律 403「企业已被平台停用」
  - 前端：身份下拉加「平台超管（租户管理）」；`TABS.platform`→`tabTenants()`（开通表单+企业列表卡片+停用/启用）；roleHeaders platform 分支
- 测试 **89/89**（+TestPlatformAdmin×4：列表/开通/幂等、停用封禁与恢复、平台接口越权 403、404/400 校验）。本地+chromium UI 冒烟通过（开「静安普拉提工作室」→列表出现 #4）。
- 📄 `wechat/docs/企业端v3-M3平台租户层管理-验收说明-2026-09-05.md` + 截图 `screenshots/平台租户层M3-2026-09-05/{01,02}.png`。
- ⚠️ **云端 cardcount-web 不是 push 自动部署**（18:16 的 002 是手动发布结果）：M3 push 后线上仍 404 → 需用户浏览器 cloud.weixin.qq.com/cloudrun/service/cardcount-web 点「重新部署/发布」才上线。chromium 对 cloud.weixin.qq.com 登录态已过期，无法代点。
- 本地演示(8090)22:16 重启（M3，pid 见 /tmp/cc_demo.pid，DB /tmp/cc_demo.db）。

## 22:30–23:00 商业用户体系（B 端账号）开发完成（98/98）+ 推送
- 用户拍板：覆盖平台超管+企业员工（B 端优先）；密码+不透明 Bearer（DB 会话可吊销 12h、PBKDF2 加盐）；交付 API+测试+文档（UI 仍演示身份）。
- demo 仓 commit `62350bd` 已推 GitHub main：
  - `UserAccount`(actorType platform_admin|staff, username 唯一, PBKDF2) + `AuthToken`(revokedAt/expiresAt)
  - `services/auth.py`：hash/authenticate/issue/resolve/revoke/change_password/set_account_status/actor_view
  - `routes`：_staff/_merchant_id/_platform_admin 兼容 `Authorization: Bearer`（X-* 演示头保留零回归）；新 `/api/auth/login|logout|me|password|accounts(GET/POST)|accounts/<id>/status`
  - open_merchant 自动建授权管理员账号 `admin{merchantId}`（可传/返回 initialPassword）
  - seed 预置 platform / admin1(店长小林) / clerk1(核销员小赵)，初始密码 `Demo@123456`
- 测试 **98/98**（+TestUserAuth×9）；HTTP 冒烟：登录→me→Bearer 建模板 200→演示头回归 200→platform bearer 租户 200→无 token 401→改密吊销/停用踢下线实测通过。
- 📄 `wechat/docs/用户体系-B端账号与登录-开发验证-2026-09-05.md`。
- ⚠️ 云端 cardcount-web 仍未手动「发布」（M3 62350bd 均已在 GitHub，等用户在 cloud.weixin.qq.com 点发布→003）。
