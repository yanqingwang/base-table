# 2026-08-20 工作日志

## 优化迭代：card-counter-flask 商业化并发保护 + 测试加固
- 背景：commercial.py 并入时未带 SQLite 并发保护（原独立版有全局 RLock）；SQLite 下 `with_for_update()` 是空操作，并发预约/核销可能超卖/双扣。
- 改动（`code/card-counter-flask/wxcloudrun/commercial.py`）：
  - 新增 `_WRITE_LOCK`(RLock) + `_write_locked` 装饰器 + `_is_mysql()`：MySQL 走行锁（零开销），SQLite 单进程内串行化所有类资金写操作。
  - 12 个写函数挂锁：create_single_session / create_schedule_rule / generate_sessions / patch_session / cancel_session / create_booking / cancel_booking / merchant_cancel_booking / checkin / no_show / void_card / create_refund。
- 新增 `test_commercial_concurrency.py`（7 项全过）：
  1. 并发预约不超卖：容量 2 × 10 人 → 恰好 2 confirmed + 8 waitlisted；
  2. 并发签到不双扣：同一预约 10 线程 checkin → used_times 恒 1（verify_token 幂等 + RLock）；
  3. 并发销卡幂等：同 idempotencyKey 10 线程 → 全部 200、仅 1 条 CardVoidRecord。
- 回归：集成测试 22/22、test_b_end.py 全通过（锁未破坏既有功能）。
- 新建 `wechat/README.md`（AGENTS.md 约定的权威速查：项目地图/部署机制/AppID与密钥位置/测试/商业化 API/合规闸门）。
- 环境确认：本机无 docker、无 MySQL 服务 → MySQL 多 worker 压测与镜像构建验证需在用户云环境做（留作下一步）。

## 竞品分析：次卡管家「次卡管理」功能
- 输出：`wechat/docs/次卡管家竞品分析报告-2026-08-20.md`
- 覆盖：国内（码云/乔拓云、博卡、有赞/微盟、银豹、三体云动）+ 国际（Mindbody、Zenoti、Vagaro、GlossGenius、Gymdesk），含 G2/Capterra/PH 评价汇总。
- 核心结论：赛道分层清晰；竞品"重营销轻凭证"，「合规可信 + 核销零纠纷」是空白价值锚点，与 card-counter 已建的幂等核销/行锁/审计/合规闸门高度契合。
- 推荐差异化：方向1 安心次卡·合规版 + 方向2 核销零纠纷（护城河）、方向3 微信原生极简+透明低价（获客）、方向4 行业模板一键开店（补强）。

## 消费端体验走查 + 开发待办
- 消费端走查（card-counter-flask）：`/api/my/cards` 跨商户天然支持（owner_openid 无单商户约束），但缺 ① 商户名/logo 聚合、② 顾客端核销明细端点、③ 储值卡余额模型（CardTemplate 仅 total_times 次数型）、④ 转赠/家庭共享。
- 开发待办：`wechat/tasks/开发待办事项-2026-08-20.md`，按 P0(体验补强)/P1(合规+审计护城河)/P2(行业模板)/P3(获客跟随)/V(验证) 分级，对应报告方向1-4。

## P0-2 跨商户卡包聚合（card-counter-commercial 落地+验证）
- 用户确认：目标仓库 = `code/card-counter-commercial`（商业版独立应用，已含顾客核销记录/转赠/退款/审计，比 card-counter-flask 超前；原路径"商业化的次卡管理应用"不存在）；本轮只做 P0-2。
- 改动（`src/backend/`）：
  - model.py：`Merchant` 加 `logoUrl`(String 255)；migrate() 增量补列，向前兼容。
  - services/card.py：新增 `_merchant_info` / `my_cards`（扁平 enriched，带 merchantName/merchantLogo，向后兼容）/ `my_merchants`（按商户聚合：cardCount/totalRemainingTimes）。
  - routes/__init__.py：`/api/cards/mine` 改为调 `card_svc.my_cards`；新增 `/api/my/merchants`。
  - seed.py：新增 merchant id=3（悦动瑜伽，含 logoUrl）+ 瑜伽10次卡，发卡并给演示顾客领取；merchant1 补 logoUrl。（注意：曾误用 id=2 与 TestPermission 的 `Merchant(id=2)` 冲突，已改为 3；变量名 `c2` 也与 Course 的 c2 冲突，已改名 m2_card。）
- 验证：`python -m unittest backend.tests.test_core`（PYTHONPATH=src）→ 25/25 通过；新增 TestCustomerCardPack（3 项：卡带商户信息、跨商户分组、空顾客空聚合）；migrate 向前兼容测试仍过。
- 关键认知：`card-counter-commercial` 的 `card_svc.*` 返回 dict（如 issue_card/claim_card），而 `sched.create_course` 返回 ORM 对象——seed 里变量命名需避开 c1/c2 等被后续复用名。

## 下次起点（更新）
- 腾讯云托管部署验证（USE_MYSQL=true + MYSQL_*；本机无 MySQL/docker，需云环境）
- 网页 Demo 适配 JWT 或 miniapp 对接新端点
- MySQL 多 worker 并发压测（100 并发预约/核销，行锁验证）
- 合规：预付卡备案 → 资金存管 → 微信支付资质 → 退费规则

## P0-3 储值卡 + P0-4 提醒（card-counter-commercial）
- P0-3：CardTemplate 加 cardKind(times/stored)+valueCents；Card 加 cardKind/faceValueCents/balanceCents；RedemptionRecord 加 amountCents。recompute_card_aggregates 支持余额重算；_redeem 按 cardKind 分支（stored 按额扣减+幂等+审计）。seed 加储值模板+卡。验证 5 项全过。
- P0-4：新增 services/notify.py（scan_reminders 到期7天/余次≤3/余额≤50元，按日+卡+模板幂等去重；list_for_customer）。routes 加 GET /api/notifications + POST /api/merchant/notifications/scan。占位落库 NotificationLog，接微信订阅消息时替换 _send() 即可。验证 3 项全过。
- 踩坑：_send 从 payload(字符串)取 triggerDate 再调 isoformat() 报错 → 改为接收 date 对象参数。
- 全量测试 33/33 通过（25 旧 + 8 新），migrate 向前兼容无回归。
- P0 系列全部完成：P0-1(商业版自带)/P0-2(跨商户卡包)/P0-3(储值)/P0-4(提醒)。

## P1 护城河（card-counter-commercial，44/44 通过）
- 用户指令："继续P1所有任务的开发和验证"。完成 P1-1 合规包 / P1-2 纠纷凭证导出 / P1-3 异常核销预警；P1-4 后端聚合完成，前端留待 card-counter-miniapp 另仓走查。
- P1-1：Merchant 加合规字段（compliancePackEnabled / prepaidFilingStatus+note / fundCustodyStatus+provider / wechatPayCategoryStatus+note）。routes 加 GET/POST /api/merchant/compliance（自检清单 + 配置，需 manage_staff）。顾客端 `/api/cards/<id>/customer-detail` 透出 refundRule + 合规徽标(verified/partial/none)。seed 商户1 设为 partial。
- P1-2：新增 services/export.py。`export_card_excel` 用**纯标准库 zipfile+XML** 生成真实 .xlsx（无需 openpyxl，规避 requirements 未声明该依赖）；`export_card_pdf` 优先 reportlab、未装则回退纯标准库最小 PDF（保证 %PDF 产出）。商户端 `GET .../export?format=pdf|excel`(view_report)；顾客端 `GET .../voucher` 摘要。
- P1-3：新增 AlertEvent 模型 + services/alerts.py（raise_alert 同事务 / raise_alert_detached 独立事务 / list / resolve / manual_flag，按日去重）。自动 hook：void_card 异常作废(critical)+有余额非退款作废；_redeem 活跃卡超额拦截(over_redeem_blocked) → 路由层在主事务回滚后独立落告警（关键：避免 SQLite 锁冲突）。商户端 alerts 列表/标记飞单/处理。
- 测试：新增 TestP1 11 项；全量 **44/44**（原 33 + 11）。
- 踩坑复盘（重要，可复用）：
  1. `from ..db import _engine` 在模块顶层会捕获 import 时的 None，init_db 重赋值后该引用仍是 None → 必须在函数内动态读 `db._engine`（或调用时 import）。
  2. 跨事务写告警：被拦截的核销主事务会 rollback，若在同一事务内落告警会被一起回滚；必须在路由 `except` 块（主事务已回滚、锁已释放）用独立 session 提交，否则 SQLite "database is locked"。
  3. 路由里 `with atomic(session) as s:` 必须绑定 `as s`，否则内部引用的 `s` 未定义 → 500。
  4. 路由用 `_snake` 后请求键变 snake_case（如 voidType→void_type），取参要用小写键。
  5. 给 BizError 加了可选 detail 字段（REDEEM_BLOCKED 携带 cardId/merchantId 给路由落告警用）。
- 生产注意：reportlab/openpyxl 未写进 requirements.txt；Excel 已零依赖，PDF 在 slim 环境走最小 PDF 回退（精度有限，要高精度排版可 `pip install reportlab`）。
