# Base Table 安装与使用简明手册

## 运行位置

所有命令在项目根目录执行：

```bash
cd /home/wang/wk/code/base-table
```

## 开发运行

```bash
npm run tauri:dev
```

也可以只启动前端预览：

```bash
npm run dev
```

## Arch 安装包

重新生成 Arch 安装包：

```bash
./packaging/arch/build-arch-package.sh
```

该脚本只执行前端构建、Tauri 桌面二进制构建和 `makepkg`，不会生成 Windows、Debian 或 AppImage 包；其他平台包只在 GitHub Release 工作流中生成。

安装：

```bash
sudo pacman -U packaging/arch/base-table-0.1.0-1-x86_64.pkg.tar.zst
```

安装后运行：

```bash
base-table
```

## 基础使用

1. 点击 `新建 Base` 创建一个文件。
2. 可用文件旁边的 `⇄` 把文件移动到指定文件夹。
3. 点击 `新建表` 创建表格。
4. 点击 `新增列` 创建字段，字段类型支持 `text`、`number`、`date`、`bool`、`single_select`。
5. 字段旁的 `选项` 可为 `single_select` 配置可选项，多个值用英文逗号分隔。
6. 字段旁的 `校验规则` 可填写正则表达式，编辑单元格时会校验。
7. 点击 `新增记录` 后，可在表格单元格中直接编辑数据。
8. `date` 类型字段会使用日期输入框编辑，字段设置里的 `日期格式` 可控制显示格式。
9. `number` 类型字段可设置为整数、小数、货币或百分比，并配置小数位数与货币代码。
10. `formula` 类型字段可填写 `{列名}*{列名}` 形式的公式，应用会基于当前行计算结果。
11. 点击 `列行转换` 会切换到单独的转置视图，不会生成新表；可在顶部选择哪一列作为转置后的标题列。
12. 点击 `导出数据` 可把当前表或当前保存视图结果按字段顺序导出为 CSV；桌面版会弹出保存文件对话框。
13. `列设置`、`DeepSeek AI` 设置、`保存视图/筛选设置` 都可点击 `隐藏设置` 收起，点击 `显示设置` 展开；列设置展开时还可拖动面板右侧竖条缩小或放大。
14. 当数据列较多、一个界面无法完整显示时，表格会自动撑开并显示横向滚动条。
15. 左侧菜单按 Base、表格、视图三层分组；每个菜单组都可以隐藏和展开，保存视图会显示在视图菜单中。

## Excel 导入

点击 `导入 Excel` 后会弹出系统文件选择对话框，选择 `.xlsx`、`.xlsm` 或 `.xls` 文件。若只运行 `npm run dev` 的浏览器预览版，则会退回为路径输入；桌面版 `npm run tauri:dev` 和安装包会使用原生文件选择对话框。

- 每个 sheet 会自动创建为一张表。
- 首行自动作为字段名。
- 导入时会自动推断字段类型和候选维度。
- Excel 日期单元格会按日期处理，避免导入为 Excel 序列数字。

## 视图

- `表格`：直接维护行列数据。
- `看板`：可选择分组字段，并勾选卡片需要显示的字段。
- `甘特`：当表中存在 `date` 类型字段时，会按开始日期和结束日期生成甘特条；顶部可选择开始/结束日期列，并切换按日、周、月、季度或年度显示。看板、联合视图、转置视图和分析视图都会使用字段统一日期格式。
- `分析`：选择维度字段、数值指标和可选第二指标后，可生成汇总分析视图；支持柱状、条形和双指标展示。
- `列行转换`：以视图方式展示字段和记录互换后的结果，不改变原表结构。
- `统计`：工作区上方显示行数、列数、填充率、候选维度数量。

## 保存视图

- 在 `保存视图` 面板选择字段、筛选条件和值后，点击 `保存为视图` 可保存筛选视图，例如未来 60 天、优先级等于 High。
- 保存视图也支持选择排序字段和升序/降序方向，适合按日期、优先级、金额等字段组织数据。
- 选择已保存视图后，可点击 `更新视图` 覆盖筛选条件，或点击 `重命名` 修改视图名称。
- 联合视图可选择关联表、主表匹配列和关联表匹配列，点击 `保存联合视图` 后会以 left join 方式生成只读联合视图。

## DeepSeek AI

- 在 `DeepSeek AI` 面板点击 `DeepSeek API Key` 绑定按钮，可弹出对话框保存 API Key；再输入操作说明后，可让 AI 返回结构化 JSON 更新。
- `处理公式` 是独立按钮，不需要 DeepSeek API Key；点击后会把当前公式字段结果写入记录数据。
- 当前支持两类安全更新：更新列配置（例如数字格式、公式、校验规则）和更新指定记录的指定字段值。
- API Key 仅保存在本机浏览器/Tauri WebView 的 `localStorage` 中；AI 返回结果会先校验 JSON 结构，再应用到本地数据。

## 验证命令

```bash
npm run test
npm run build
cd src-tauri && cargo fmt --check && cargo test
```
